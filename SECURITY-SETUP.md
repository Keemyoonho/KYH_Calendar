# 본인 전용 접근 설정

현재 서버는 사용자가 게시한 전체 접근 차단 규칙을 유지합니다. 이 파일은 서버 설정을 자동 변경하지 않습니다.

1. Firebase Authentication에서 Google 제공업체를 활성화하고 지원 이메일을 선택합니다.
2. Authentication 설정의 승인된 도메인에 `keemyoonho.github.io`를 추가합니다. 경로와 https는 넣지 않습니다.
3. 사이트에서 본인 Google 계정으로 로그인하고 화면에 표시되는 Firebase UID를 확인합니다.
4. 확인한 UID를 `js/security.js`의 OWNER_UID에 지정하여 배포합니다. 빈 값이면 누구에게도 접근을 허용하지 않습니다.
5. Realtime Database 규칙을 아래처럼 본인 UID로만 허용합니다. 예시 문자열을 실제 확인한 UID로 바꾸어야 합니다. `auth != null`만으로 허용하거나 true로 열지 않습니다.

```json
{
  "rules": {
    ".read": false,
    ".write": false,
    "calendar": {
      ".read": "auth != null && auth.uid === 'REPLACE_WITH_OWNER_UID'",
      ".write": "auth != null && auth.uid === 'REPLACE_WITH_OWNER_UID'"
    }
  }
}
```

6. 규칙 플레이그라운드에서 미인증/다른 UID 읽기·쓰기 거부, 본인 UID 허용을 확인합니다. 실제 기록에 쓰기 테스트를 하지 않습니다.
7. 본인 계정으로 기존 서버 기록을 읽을 수 있는지 확인합니다. 로그인만 성공하고 규칙이 차단되어 있으면 화면은 잠겨 있고 로컬 기록을 서버에 업로드하지 않습니다.

클라이언트 UID 검사는 사용 편의를 위한 것이며, 실제 보안 경계는 Firebase 규칙입니다. UID는 비밀번호가 아니지만 비밀번호·토큰·서비스 계정 키는 공유하지 않습니다.

일기는 본인 UID 전용 규칙이 적용되는 calendar/diaryRecords에 날짜별로 저장합니다. 기존 브라우저 원본과 동시 수정 충돌본은 calendar/diaryImports에 보관합니다. 일반 캘린더 저장은 update를 사용하여 이 경로들을 덮어쓰지 않습니다. 기존 브라우저 데이터는 자동 삭제하지 않습니다. 이전 완료와 서버 저장 완료를 확인한 뒤에만 브라우저 데이터를 삭제합니다. 미전송 초안은 브라우저에 남아 있으므로 공용 기기를 사용하지 않습니다. 일기는 종단 간 암호화되지 않으며, Firebase 프로젝트 관리자도 접근할 수 있습니다.

이번 변경은 일정 제목/메모/마감 메모/링크의 HTML 해석과 위험 URL 실행을 차단합니다. 테스트는 가짜 데이터와 차단된 네트워크 환경에서 수행합니다. 서버 규칙은 콘솔에서 별도로 게시하고 검증해야 합니다.
